<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To-Do Manager</title>
    <!-- Correct Tailwind CSS CDN link -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Custom CSS -->
    <link rel="stylesheet" href="/css/styles.css" />
</head>

<body>
    <div class="grid grid-cols-6">
        <!-- Centering the content -->
        <div class="col-start-3 col-span-2">
            <!-- Include header partial correctly -->
            <%- include('header.ejs') %>
            <!-- To-Do Form -->
            <form action="/todos" method="POST">
                <div class="flex gap-2 py-4">
                    <!-- Input for To-Do text -->
                    <div class="flex-auto">
                        <input type="text" name="title" placeholder="What's next?" class="border border-gray-300 rounded text-gray-900 w-full p-2 text-sm" required />
                    </div>
                    <!-- Input for date -->
                    <div class="flex-auto">
                        <input type="date" name="dueDate" class="border border-gray-300 rounded text-gray-900 w-full p-2 text-sm leading-4" required />
                    </div>
                    <!-- Submit Button -->
                    <div class="flex-none">
                        <button type="submit" class="bg-green-600 text-white px-5 py-1.5 rounded font-medium mr-2 md-2">Add</button>
                    </div>
                </div>
            </form>

            <!-- Include the todos.ejs partial to render the to-dos -->
            <%- include('todos.ejs', { overdueTodos, dueTodayTodos, dueLaterTodos }) %>
        </div>
    </div>
</body>

</html>
