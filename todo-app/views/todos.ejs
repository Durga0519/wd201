<!-- Overdue Section -->
<div class="flex items-center">
    <h5 class="flex-none">Overdue</h5>
    <span id="count-overdue" class="ml-2 text-gray-600 flex-none"><%= overdueTodos.length %></span>
</div>
<ul>
    <% overdueTodos.forEach(function(todo) { %>
        <li class="Todo-Item">
            <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-red-50">
                <input type="checkbox" class="w-4 h-4 text-blue-600 bg-green-100 rounded border-gray-300" />
                <label class="ml-2 text-sm text-gray-600 cursor-pointer">
                    <%= todo.title %> (Due on <%= todo.dueDate %>)
                </label>
            </div>
        </li>
    <% }); %>
</ul>

<!-- Due Today Section -->
<div class="flex items-center">
    <h5 class="flex-none">Due Today</h5>
    <span id="count-due-today" class="ml-2 text-gray-600 flex-none"><%= dueTodayTodos.length %></span>
</div>
<ul>
    <% dueTodayTodos.forEach(function(todo) { %>
        <li class="Todo-Item">
            <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-green-50">
                <input type="checkbox" class="w-4 h-4 text-blue-600 bg-green-100 rounded border-gray-300" />
                <label class="ml-2 text-sm text-gray-600 cursor-pointer">
                    <%= todo.title %> (Due Today)
                </label>
            </div>
        </li>
    <% }); %>
</ul>

<!-- Due Later Section -->
<div class="flex items-center">
    <h5 class="flex-none">Due Later</h5>
    <span id="count-due-later" class="ml-2 text-gray-600 flex-none"><%= dueLaterTodos.length %></span>
</div>
<ul>
    <% dueLaterTodos.forEach(function(todo) { %>
        <li class="Todo-Item">
            <div class="flex items-center w-fit my-2 px-2 py-1 rounded hover:bg-blue-50">
                <input type="checkbox" class="w-4 h-4 text-blue-600 bg-green-100 rounded border-gray-300" />
                <label class="ml-2 text-sm text-gray-600 cursor-pointer">
                    <%= todo.title %> (Due on <%= todo.dueDate %>)
                </label>
            </div>
        </li>
    <% }); %>
</ul>
